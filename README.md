# Daniel & Ariane Surprise Site

This is a static Vercel-ready website.

Upload these files as the root of the GitHub repository:

- `index.html`
- `styles.css`
- `script.js`
- `assets/`

The music is inside `assets/song.mp3`. Browsers only allow it to play after a visitor taps the Yes button.
